params:
  epochs: 30
  batch_size: 32
  no_classes: 10
  input_shape: [28,28]
  loss_function: sparse_categorical_crossentropy
  metrics: accuracy
  optimizer: SGD
  validation_datasize

artifacts:
  artifacts_dir: artifacts
  model_dr: model
  plot: plots
  checkpoint_dir: checkpoints

logs:
  log_dir: log_dir
  general_logs: general_logs
  tensorbord_logs: tensorboard_logs